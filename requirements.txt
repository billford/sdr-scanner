anthropic>=0.40.0
openai-whisper>=20231117
requests>=2.31.0
# pydub is optional — used for more accurate silence detection if you want to swap in
# pydub>=0.25.1
# ffmpeg must be installed separately: brew install ffmpeg
